import React from "react";
import { Col, Container, Image, Row, Stack } from "react-bootstrap";

// Define the functional component About with TypeScript
const About: React.FC = () => {
  return (
    <>
    </>
  );
};

export default About;
